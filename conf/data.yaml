clean:
  # merged canonical file for pubmed+ctgov+openfda
  main_corpus: "data/cleaned/corpus.jsonl"
  kaggle: "data/clean/kaggle_merged.jsonl"

corpus:
  out: "data/corpus/corpus.jsonl.gz"
  min_chars: 20
  lang: "en"
  shuffle: true
